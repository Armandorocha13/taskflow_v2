spring.application.name=taskflow-backend

# Configurações de Banco de Dados (Supabase - Usando CONNECTION POOLER porta 6543)
# O Pooler é mais estável para conexões saindo do Render
spring.datasource.url=jdbc:postgresql://db.gackvwuokitcfvkrgngt.supabase.co:6543/postgres?sslmode=require
# No Pooler, o usuário deve incluir o ID do projeto: postgres.[PROJECT_REF]
spring.datasource.username=postgres.gackvwuokitcfvkrgngt
spring.datasource.password=ACeVwrBDv9hNgSbU
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do Pool (Hikari)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080
